# Auto detect text files and perform LF normalization
* text=auto

# Ensure IPv6 shard JSONs are committed as regular blobs (no LFS)
public/ip2l6/** -text -filter -merge -diff
public/ip2l6/**/*.json filter=lfs diff=lfs merge=lfs -text
data/IP2LOCATION-LITE-DB3.IPV6.CSV filter=lfs diff=lfs merge=lfs -text
